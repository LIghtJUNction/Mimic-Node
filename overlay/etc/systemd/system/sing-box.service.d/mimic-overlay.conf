[Unit]
# Ensure the configuration overlay is mounted before sing-box starts.
# This prevents sing-box from reading the underlying directory directly.
Requires=mimic-node-mount.service
After=mimic-node-mount.service
